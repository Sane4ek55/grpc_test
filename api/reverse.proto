syntax = "proto3";

package Sane4ek55.grpc_test.api;

option go_package = "/;revers";

service Reverse {
    rpc Do(RequestReverse) returns (ResponseReverse);
}

message RequestReverse {
    string message = 1;
}

message ResponseReverse {
    string message = 1;
}